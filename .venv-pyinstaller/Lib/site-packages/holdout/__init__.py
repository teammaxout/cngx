"""holdout — an independent significance referee for LLM & agent evals.

Is your improvement real, or noise? Paired test + variants-aware correction + confidence
intervals, done correctly. The stats are open source; the hosted service adds independence,
a write-once holdout you can't tune against, and a verifiable badge (https://holdout.dev).

    from holdout import compare
    res = compare(baseline_scores, candidate_scores, variants_tried=37)
    print(res.report())
    assert res.significant   # or gate your CI on it
"""
from __future__ import annotations

__version__ = "0.1.0"

from .compare import ComparisonResult, compare
from .corrections import benjamini_hochberg, bonferroni, sidak
from .stats import (
    bootstrap_diff_ci,
    fwer,
    mcnemar_counts,
    mcnemar_exact,
    min_detectable_effect,
    paired_permutation_test,
    proportion_se,
    required_n,
)

__all__ = [
    "__version__",
    "compare",
    "ComparisonResult",
    "paired_permutation_test",
    "mcnemar_exact",
    "mcnemar_counts",
    "bootstrap_diff_ci",
    "proportion_se",
    "fwer",
    "required_n",
    "min_detectable_effect",
    "bonferroni",
    "sidak",
    "benjamini_hochberg",
]
